<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lamda.demo.mappers.MovieMapper">
    <insert id="insertMovie"
            parameterType="com.lamda.web.movie.MovieDTO">
        insert rank, title, rank_date into movie
        values (#{seq},#{movieName},#{rankDate})
    </insert>

    <update id="updateMovie"
            parameterType="com.lamda.web.movie.MovieDTO">
        update set movieName = #{movieName} from movie
        where movie_no like #{movieNo}
    </update>

    <delete id="deleteMovie"
            parameterType="com.lamda.web.movie.MovieDTO">
        delete from movie where seq like #{seq}
    </delete>
    <select id="selectMovies" resultType="com.lamda.web.movie.MovieDTO">
       select movie_no movieNo, rank, movieName, rank_date rankDate
      from movie

    </select>
    <select id="selectMovie" resultType="com.lamda.web.movie.MovieDTO">
      select movie_no movieNo, rank, title, rank_date rankDate
      from movie
      where movie_no = #{movieNo}
    </select>
    <select id="count" resultType="int">
      select count(*)
      from movie
    </select>
</mapper>